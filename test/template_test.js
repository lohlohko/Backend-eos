const assert = require('assert');
const sinon = require('sinon');

describe('parent', () => {
    beforeEach(() => {
        // setup code here
    });

    afterEach(() => {
        // cleanup code here
    });

    describe('#child', () => {
        it('should error', async () => {
            // test code here
        });

        it('should be success', async () => {
            // test code here
        });
    });
});